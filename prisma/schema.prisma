generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  id        String     @id @default(uuid())
  email     String     @unique
  firstname String
  lastname  String
  password  String
  username  String?    @unique
  phone     String?
  location  String?
  image     String?
  bio       String?
  status    UserStatus
  joinedAt  DateTime   @default(now())
  roleId    String
  role      Role       @relation(fields: [roleId], references: [id])
  vendors   Vendors?
  bookings  Bookings[]
}

model Vendors {
  id                  String         @id @default(uuid())
  name                String
  locations           Location[]     @relation("VendorLocations")
  defaultLocationId   String?        @unique
  defaultLocation     Location?      @relation("VendorDefaultLocation", fields: [defaultLocationId], references: [id])
  services            Service[]
  description         String?
  image               String?
  phone               String?
  photos              String?
  cancellation_policy String?
  categoryId          String
  category            Category       @relation(fields: [categoryId], references: [id])
  planId              String
  plan                Plans          @relation(fields: [planId], references: [id])
  status              AccountStatus  @default(ACTIVE)
  joinedAt            DateTime       @default(now())
  trialEndsAt         DateTime?
  subscriptionProfessionalCount Int @default(1)
  subscriptionLocationCount     Int @default(1)
  businessHours       BusinessHour[]
  userId              String         @unique
  user                Users          @relation(fields: [userId], references: [id])
  isComplete          Boolean        @default(false)
  professionals       Professional[]
}

model Category {
  id      String    @id @default(uuid())
  name    String    @unique
  image   String?
  vendors Vendors[]
}

model Plans {
  id            String    @id @default(uuid())
  name          String    @unique
  price         Float
  duration      Int
  trial_period  Int
  billing_cycle String
  createdAt     DateTime  @default(now())
  professional  Int       @default(1)
  extraProfessionalPrice Float @default(0)
  vendors       Vendors[]
  location      Int       @default(1)
  extraLocationPrice Float @default(0)
}

model Role {
  id    String  @id @default(cuid())
  name  String  @unique
  users Users[]
}

model Bookings {
  id          String        @id @default(cuid())
  date        DateTime
  createdAt   DateTime      @default(now())
  status      BookingStatus @default(PENDING)
  scheduledAt DateTime
  userId      String
  user        Users         @relation(fields: [userId], references: [id])
  serviceId   String
  service     Service       @relation(fields: [serviceId], references: [id])
}

model Invitation {
  id        String   @id @default(uuid())
  email     String   @unique
  token     String
  expiresAt DateTime
  used      Boolean  @default(false)
}

model Location {
  id       String  @id @default(cuid())
  name     String?
  phone    String?
  photos   String?
  vendorId String
  vendor   Vendors @relation("VendorLocations", fields: [vendorId], references: [id])
  defaultFor Vendors? @relation("VendorDefaultLocation")

  address   String
  latitude  Float?
  longitude Float?

  offerAtClient   Boolean @default(false)
  offerAtBusiness Boolean @default(false)

  travelFee         Float @default(0.0)
  maxTravelDistance Float @default(5.0)

  isActive  Boolean   @default(true)
  isDefault Boolean   @default(false)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  services  Service[]
  professionals Professional[]
  businessHours BusinessHour[]

  @@index([vendorId])
}

model Service {
  id          String     @id @default(cuid())
  name        String
  description String?
  vendorId    String
  vendor      Vendors    @relation(fields: [vendorId], references: [id])
  locationId  String
  location    Location   @relation(fields: [locationId], references: [id])
  price       Float
  duration    Int
  booking     Bookings[]

  @@unique([name, vendorId, locationId])
}

model Professional {
  id        String             @id @default(uuid())
  name      String
  email     String?            @unique
  phone     String?
  vendorId  String
  vendor    Vendors            @relation(fields: [vendorId], references: [id])
  locationId String?
  location  Location?          @relation(fields: [locationId], references: [id])
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt
  roleId    String
  role      ProfessionalRole   @relation(fields: [roleId], references: [id])
  status    ProfessionalStatus @default(INACTIVE)
}

model ProfessionalRole {
  id            String         @id @default(cuid())
  name          String         @unique
  professionals Professional[]
}

model BusinessHour {
  id        String   @id @default(cuid())
  vendorId  String
  vendor    Vendors  @relation(fields: [vendorId], references: [id])
  locationId String?
  location  Location? @relation(fields: [locationId], references: [id])
  day       String
  isOpen    Boolean  @default(false)
  openTime  String?
  closeTime String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([locationId, day])
  @@index([vendorId])
}

enum UserStatus {
  ACTIVE
  INACTIVE
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELED
  COMPLETED
}

enum AccountStatus {
  TRIAL_ACTIVE
  TRIAL_EXPIRING
  TRIAL_EXPIRED
  ACTIVE
  INACTIVE
  PENDING
}

enum ProfessionalStatus {
  ACTIVE
  INACTIVE
  PENDING
}
